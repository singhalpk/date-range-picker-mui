import React from 'react';
import DateRangePickerWrapper from './date-range-picker-wrapper';

const DateRangePickerExporter = props => <DateRangePickerWrapper {...props} />;

export default DateRangePickerExporter;
